import datetime

# Obtener el día de la semana en español
dias_semana = ['lunes', 'martes', 'miércoles', 'jueves', 'viernes', 'sábado', 'domingo']
dia_actual = datetime.datetime.now().weekday()  # 0 = lunes, 6 = domingo
archivo_entrada = f'{dias_semana[dia_actual]}.txt'  # Archivo correspondiente al día de la semana

# El resto de tu código se mantiene igual
archivo_salida = 'horario.html'  # Nombre del archivo de salida
convertir_horario_a_html(archivo_entrada, archivo_salida)
print(f'Se ha generado el horario en HTML: {archivo_salida}')
